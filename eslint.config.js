export default {
  root: true,
  env: {
    node: true,
    browser: true,
    es2021: true,
  },
  extends: [
    "eslint:recommended",
    // additional shared configurations
  ],
  rules: {
    // your custom rules
  },
};
